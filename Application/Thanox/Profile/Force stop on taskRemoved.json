[
    {
        "name":"Force Stop on Task Removed",
        "description":"Force stops the app instantly when removed from recent tasks. Safe for System Apps.",
        "priority":1,
        "condition":"taskRemoved == true && task.hasTaskFromPackage(pkgName) == false && !globalVarOf$whitelist.contains(pkgName) && activity.getFrontAppPackageComponent().getPackageName() != pkgName",
        "actions":[
            "ui.showShortToast(\"🛑 Closing \" + thanos.pkgManager.getAppInfo(pkgName).appLabel);",
            "sh.exe('am force-stop ' + pkgName)"
        ]
    }
]